{
    "poll_interval":1,
    "update_interval": null,
    "sys_info":{
        "name":"aurora",
        "ncores_per_node":104,
        "ngpus_per_node":12
    },
    "ensembles":{
        "name1":{
            "num_nodes":1,
            "num_processes_per_node":1,
            "launcher":"mpi",
            "relation":"one-to-one",
            "pre_launch_cmd":"echo 'Pre-launch command executed'",
            "cmd_template":"./test_script.sh -l {opts1} -h {opts}",
            "opts1":"linspace(0, 208, 208)",
            "opts":"linspace(0, 208, 208)",
            "run_dir":"./run_dir"
        }
    }
}
